import { MERIDIA_PARAMS, type CountryParams } from '@terrarium/engine'

/** Fixed mid-poor 1946 country for tests — never regenerate, never tweak
 * casually: golden replays hash states built from this. */
export const standardCountry: CountryParams = MERIDIA_PARAMS
